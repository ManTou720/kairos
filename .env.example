# Postgres connection string.
# Local Docker:   postgres://kairos:kairos@localhost:5432/kairos
# Neon (pooled):  postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/kairos?sslmode=require
DATABASE_URL=postgres://kairos:kairos@localhost:5432/kairos

# Optional: point the frontend at an external API instead of the
# built-in route handlers (app/api). Leave empty for same-origin.
NEXT_PUBLIC_API_URL=
